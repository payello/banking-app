import styled from "styled-components";

const FormContainer = styled.div`
  display: flex;
  flex-direction: row;
  max-width: 800px;
  margin: 0 auto;
  margin-bottom: 1rem;
`;

export default FormContainer;
